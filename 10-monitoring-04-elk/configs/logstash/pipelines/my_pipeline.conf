input {
  beats {
    port => 5044
    codec => json
    ssl => false
  }
}

filter {
  json { source => "message" }
}

output {
  elasticsearch {
    hosts => "es-hot:9200"
    index => "logstash-%{+YYYY.MM.dd}"
    user => "elastic"
    password => "elastic"
  }
  stdout { codec => rubydebug }
}
